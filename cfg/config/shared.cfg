//══════════════════════    跨模式公共 alias    ══════════════════════════════
// 预留位置：当前为空。
// 设计原则：每个模式享有全套键位，alias 就近定义在各自模式 cfg 内，
// 不做跨模式提取（如 +nClip/-nClip 见 modes/pt.cfg 与 modes/kz.cfg）。
//═════════════════════════════════════════════════════════════════════════════
